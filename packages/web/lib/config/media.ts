/**
 * Hero video source for the welcome screen.
 *
 * Set to a require()'d local asset (e.g. require("@/assets/video/hero.mp4"))
 * once real footage is available, or to a hosted URL string. Left null for
 * now — VideoHero falls back to the static gradient/orb background.
 */
export const HERO_VIDEO_SOURCE: string | number | null = null;
