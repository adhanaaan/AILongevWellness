{
  "name": "@aiw/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Code shared between the web app and the native WebView shell. Must stay dependency-free -- the shell resolves it via Metro rather than npm, and packages/web/api/ (Vercel functions) must never import it.",
  "exports": {
    "./bridge": "./src/bridge/index.ts"
  },
  "types": "./src/bridge/index.ts"
}
